---
id: TASK-16
title: Record a vhs demo GIF of roses with a runnable .tape script
status: In Progress
assignee:
  - '@ross'
created_date: '2026-06-30 01:51'
updated_date: '2026-06-30 01:58'
labels:
  - docs
dependencies: []
ordinal: 2014
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Produce an animated GIF that demos roses for the README using vhs (https://github.com/charmbracelet/vhs). Provide a committed .tape script the maintainer runs locally in their terminal against their own live Feedbin account, walking through the core usage: launching roses, moving across the three columns (sources -> articles -> reader), scrolling an article, and key actions (mark read/undo, open in browser), ideally ending on the all-caught-up rose. No credentials in the script (login is assumed/handled out of band). Embed the resulting GIF in the README.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A committed vhs .tape script (e.g. demo/roses.tape) records a GIF of roses when run with vhs
- [x] #2 The demo walks through the core flow: launching roses, moving across the three columns, scrolling an article, and at least one key action (mark read / undo / open in browser)
- [x] #3 The maintainer can run it locally against their own live Feedbin account; no secrets are committed (login handled out of band)
- [ ] #4 The generated demo GIF is embedded in the README
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. demo/roses.tape vhs script: config (Output demo/roses.gif, size/font/theme/typing speed), Require roses, then the flow — launch roses (assumes already logged in to Feedbin), browse Sources, focus Articles, focus Reader + scroll (reveals the scrollbar), mark read + undo (non-destructive), quit; finale: 'roses preview' to show the all-caught-up rose offline. No secrets in the script. 2. README Demo section: embed demo/roses.gif + how to (re)generate with vhs (must be logged in first). 3. Maintainer runs 'vhs demo/roses.tape' to produce + commit the GIF (completes AC#4).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Wrote demo/roses.tape (vhs); 'vhs validate demo/roses.tape' passes (exit 0). Flow: launch roses (assumes logged-in Feedbin), browse Sources, focus Articles, focus Reader + scroll (shows the scrollbar), mark read + undo (non-destructive), quit, then 'roses preview' finale showing the all-caught-up rose offline. No credentials in the script (AC#3). README gained a Demo section embedding demo/roses.gif + (re)generate instructions. AC#4 (GIF embedded) is wired up but the GIF FILE must be generated by the maintainer ('vhs demo/roses.tape', needs their Feedbin login — can't be produced here) and committed; AC#4 left unchecked until that lands. vhs + ttyd are installed locally; recording also needs ffmpeg.
<!-- SECTION:NOTES:END -->
